import React from 'react';
import Navbar from '../components/Navbar/Navbar'

const Home = () => {
    return(
        <div>
            <Navbar/>
            <h1>Hello, world!</h1>
        </div>
    );   
};

export default Home