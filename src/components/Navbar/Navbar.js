import React from 'react';
import { Nav } from './NavbarElements';

const Navbar = () => {
    return(
        <div>
            <Nav>
                <h1>Hello, world inside navbar!</h1>
            </Nav>
        </div>
    );
};

export default Navbar